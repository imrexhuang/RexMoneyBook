@model DateTime?
@{
    //p.s.DateTime的格式化是寫在ViewModels
    var value = Model.HasValue
    ? Model.Value.ToString("yyyy-MM-dd")
    : "";
}
@Html.TextBox("", value, new
{
@class = "form-control",
data_datetimepicker = "y",
data_date_format = "YYYY-MM-dd",
})